Download ADModule here:
https://raw.githubusercontent.com/samratashok/ADModule/master/Import-ActiveDirectory.ps1
https://github.com/samratashok/ADModule/blob/master/Microsoft.ActiveDirectory.Management.dll

You need to download both of above in order to run the ActiveDirectory module.

Remove the comments and then run the following commands:

Import-Module .\admod1.ps1
Import-ActiveDirectory -ActiveDirectoryModule .\Microsoft.ActiveDirectory.Management.dll


###################################### COmmands and their short outputs ##########################
Get global catalogs using ADmodule for other forests

Get-ADForest | select -ExpandProperty GlobalCatalogs

Get-ADDomainController -Filter * | Select Name,Site,IPv4Address

Get-ADUser -Filter {ServicePrincipalName -ne "$null"} -Properties ServicePrincipalName | select GiveName,SurName,UserPrincipalName,SamAccountName,SID,DistinguishedName,Name,Enabled

If krbtgt account is being used as a service account and its password is compromised, it can be used to create forged kerberos tickets and gain unauthorized access to parts of network. To prevent this, use best practices such as using a separate account for each service, rotating passwords regularly, and using strong password policies.